C# 입문 0-4

decimal타입은 소수점도 받을 수 있다. 하지만 소수점을 바로 넣으려면 Convert.ToDecimal(1.5); 이런식으로 형변환을 해서 넣줘야 하거나 나눗셈의 결과값으로 소수점을 받을 수는 있다.

한번에 소수점으로 초기화 할 수는 없다.

상수(변하지 않는 변수)를 선언할 때는 변수 앞에 const 를 붙혀준다(자바에서 final)

ex) const string str = "홍길동";