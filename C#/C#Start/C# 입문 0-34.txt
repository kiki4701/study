C# 입문 0-34

Thread는 운영체제가 CPU이 시간을 할당하는 기본 단위를 뜻한다. 논리적인 작업 처리 단위를 말한다.

Thread 객체를 생성해준 뒤 생성자의 매개변수에 메서드를 넣어줘야 한다.

Thread.Slip()을 이용하면 지연을 시킬 수 있다. Slip(1000); 하면 1초 쉬고 실행하고 를 반복하게 된다.

Start(); 를 이용하여 쓰레드를 실행시키고, Abort();를 이용하여 실행을 취소한다.